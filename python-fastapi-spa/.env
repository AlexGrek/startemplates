# Generate a strong random secret for production: e.g.
# python -c "import os,base64; print(base64.urlsafe_b64encode(os.urandom(48)).decode())"
JWT_SECRET=b30fafeb8e885b4b124307bb99165c16
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
USERS_FILE=users.yaml
# SECURE_COOKIES=1 will set Secure flag on cookies; set to 0 for local dev without https.
SECURE_COOKIES=0
